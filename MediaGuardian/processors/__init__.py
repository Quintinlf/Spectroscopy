"""Plugin package: every MediaProcessor subclass defined in a module here is
auto-discovered by plugins.py at startup. Drop in a new module (e.g.
image_processor.py) and subclass processing.MediaProcessor -- no other file
needs to change.
"""
